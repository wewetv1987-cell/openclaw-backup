# 团队编排使用示例

> 展示如何使用 agent-team-orchestration 系统协调多个代理完成任务

## 示例 1: 新功能开发

### 用户请求
```
"给我们的OpenClaw添加一个天气查询功能"
```

### Main (Orchestrator) 协调流程

#### 第1步: 创建任务
```markdown
# 任务 #001: 添加天气查询功能

**状态**: Inbox → Assigned
**优先级**: ⭐ 高
**创建时间**: 2026-02-17 04:05

## 需求
- 用户可以通过命令查询天气
- 支持城市名查询
- 显示温度、天气状况、湿度

## 计划
1. [ ] Architect: 设计技术方案
2. [ ] Researcher: 调研天气API
3. [ ] Coder: 实现功能
4. [ ] Architect: 代码审查
```

#### 第2步: 启动 Researcher 调研
```
Main → Researcher:
"调研可用的天气API，对比它们的优缺点、免费额度、准确度。
输出到 /shared/specs/research/weather-api-research.md"
```

#### 第3步: Researcher 产出
```markdown
# 天气API调研报告

## 推荐方案
**OpenWeatherMap** - 免费额度充足，文档完善

## 对比
| API | 免费额度 | 准确度 | 易用性 |
|-----|---------|--------|--------|
| OpenWeatherMap | 1000次/天 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| WeatherAPI | 500次/天 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| ... | ... | ... | ... |

[完整报告在文件中]
```

#### 第4步: Architect 设计方案
```
Main → Architect:
"根据Researcher的调研，设计天气查询功能的技术方案。
输出到 /shared/specs/weather-feature-spec.md"
```

#### 第5步: Architect 产出
```markdown
# 天气查询功能技术规范

## 技术方案
- API: OpenWeatherMap
- 实现: Node.js模块
- 命令: /weather [城市名]

## 架构
```
用户 → /weather命令 → weather.js → OpenWeatherMap API → 返回结果
```

## 接口设计
```javascript
// weather.js
async function getWeather(city) {
  const response = await fetch(`...`);
  return formatWeather(response);
}
```

[完整规范在文件中]
```

#### 第6步: Coder 实现
```
Main → Coder:
"根据 /shared/specs/weather-feature-spec.md 实现天气查询功能。
输出到 /shared/artifacts/code/weather/"
```

#### 第7步: Coder 交接
```markdown
## 代码交接报告

**任务**: #001 天气查询功能
**状态**: Review

### 实现内容
- 创建了 weather.js 模块
- 实现了 getWeather() 函数
- 添加了错误处理
- 编写了单元测试

### 产出文件
- `/shared/artifacts/code/weather/weather.js`: 主模块
- `/shared/artifacts/code/weather/weather.test.js`: 测试文件
- `/shared/artifacts/code/weather/README.md`: 使用说明

### 验证方法
```bash
cd /shared/artifacts/code/weather
npm install
npm test
```

### 已知问题
- 暂不支持中文城市名（需要后续添加）

### 下一步
请Architect审查代码质量和安全性
```

#### 第8步: Architect 审查
```markdown
## 代码审查反馈

**审查者**: Architect
**任务**: #001
**代码路径**: /shared/artifacts/code/weather/

### ✅ 优点
- 代码结构清晰
- 错误处理完善
- 测试覆盖充分

### ⚠️ 需要修改
1. **安全**: API Key应该从环境变量读取
   - 位置: weather.js:5
   - 建议: 使用 process.env.WEATHER_API_KEY
   - 优先级: 高

### 决定
需要修改 → 返回In Progress
```

#### 第9步: Coder 修改并重新提交
```markdown
[Coder修改代码后]

## 重新提交

已修改:
- ✅ API Key现在从环境变量读取
- ✅ 添加了 .env.example 文件

请重新审查。
```

#### 第10步: Architect 批准
```markdown
## 审查通过

**决定**: Done

所有问题已解决，代码质量良好，可以部署。
```

#### 第11步: Main 通知用户
```
✅ 任务 #001 完成: 天气查询功能

功能已实现并通过审查，现在可以使用:
/weather Bangkok

技术细节:
- API: OpenWeatherMap
- 支持: 英文城市名
- 需要配置: WEATHER_API_KEY 环境变量

查看代码: /shared/artifacts/code/weather/
```

---

## 示例 2: 定时任务（Automator）

### 用户请求
```
"每天早上9点给我发送任务进度报告"
```

### Main 协调流程

#### 第1步: 评估任务
```markdown
这是一个自动化任务 → 分配给 Automator
不需要复杂推理 → 使用 GLM-4.5
需要Coder审查 → 确保脚本质量
```

#### 第2步: 启动 Automator
```
Main → Automator:
"创建一个每日9点运行的脚本，生成任务进度报告并发送给用户。
输出到 /shared/artifacts/scripts/daily-report/"
```

#### 第3步: Automator 产出
```bash
#!/bin/bash
# daily-report.sh - 每日任务报告

# 配置
REPORT_DATE=$(date '+%Y-%m-%d')
REPORT_FILE="/tmp/daily-report-${REPORT_DATE}.md"

# 生成报告
cat > $REPORT_FILE << EOF
# 每日任务报告 - $REPORT_DATE

## ✅ 已完成
$(grep "✅" /workspace/tasks/COMPLETED.md | tail -5)

## 🔄 进行中
$(grep -A 5 "进行中" /workspace/tasks/QUEUE.md)

## ⏸️ 阻塞
$(grep -A 5 "⏸️" /workspace/tasks/QUEUE.md)

---
*生成时间: $(date '+%Y-%m-%d %H:%M:%S')*
EOF

# 发送给用户（通过OpenClaw消息API）
curl -X POST http://localhost:3000/api/message \
  -H "Content-Type: application/json" \
  -d "{\"to\": \"user\", \"message\": \"$(cat $REPORT_FILE)\"}"

echo "报告已发送"
```

#### 第4步: Coder 审查
```markdown
## 脚本审查

**审查者**: Coder
**脚本**: daily-report.sh

### ✅ 优点
- 逻辑清晰
- 有错误处理（curl）

### ⚠️ 建议
1. 添加错误处理（如果文件不存在）
2. 使用日志记录

### 决定
通过 ✅

[添加了小的改进建议，但不阻塞部署]
```

#### 第5步: 部署
```
Main:
"脚本已通过审查。添加到cron:"

# crontab -e
0 9 * * * /shared/artifacts/scripts/daily-report/daily-report.sh
```

---

## 示例 3: 并行研究（多个Researcher）

### 用户请求
```
"调研一下Python的异步框架，FastAPI vs Sanic vs Starlette"
```

### Main 协调流程（如果需要深度调研）

#### 方案A: 单个Researcher
```
Main → Researcher:
"调研FastAPI、Sanic、Starlette三个框架，
对比性能、易用性、社区活跃度、学习曲线。
输出到 /shared/specs/research/async-frameworks/"
```

#### 方案B: 并行研究（如果需要更深入）
```
Main → Researcher-A (spawn):
"深度调研FastAPI：性能测试、最佳实践、生产案例。
输出到 /shared/specs/research/async-frameworks/fastapi.md"

Main → Researcher-B (spawn):
"深度调研Sanic：性能测试、最佳实践、生产案例。
输出到 /shared/specs/research/async-frameworks/sanic.md"

[等待两个完成后]

Main → Researcher (合并):
"合并FastAPI和Sanic的调研结果，添加Starlette的分析，
产出最终对比报告。
输出到 /shared/specs/research/async-frameworks/comparison.md"
```

---

## 示例 4: 紧急任务插队

### 当前状态
```
任务 #002: 正在进行中 (Coder正在编写用户认证功能)
```

### 用户新请求
```
"🔥 紧急！生产环境有个Bug，用户无法登录，马上修复！"
```

### Main 处理流程

#### 第1步: 标记插队
```markdown
# 任务 #003: 修复生产环境登录Bug

**状态**: Inbox → 🔥 紧急
**优先级**: 🔥 紧急
**影响**: 生产环境

## 当前任务
- ⏸️ #002 用户认证功能 (暂停)
```

#### 第2步: 通知Coder
```
Main → Coder:
"⚠️ 紧急任务插队

暂停当前任务 #002，立即处理 #003 生产Bug。

当前任务状态已保存，完成紧急任务后继续。"
```

#### 第3步: 执行紧急任务
```
[Coder处理Bug...]

Coder → Main:
"✅ Bug已修复，是JWT验证的问题，已hotfix到生产环境。

详情: /shared/artifacts/code/hotfix-login-20260217/"
```

#### 第4步: 恢复原任务
```
Main → Coder:
"✅ 紧急任务已完成。

恢复任务 #002 用户认证功能，从中断点继续。"
```

---

## 总结: 关键原则

1. **Orchestrator不执行** - Main只协调，不写代码
2. **必须审查** - 所有非平凡任务都要经过Review
3. **不能自审** - Architect不能审查自己的设计
4. **明确交接** - 产出位置 + 验证方法 + 已知问题
5. **及时上报** - 阻塞超过10分钟必须escalate
6. **插队机制** - 🔥紧急任务可以打断当前工作

---
*创建时间: 2026-02-17*
